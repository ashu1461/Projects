#include<stdio.h>
#include<stack>
#include<cstring>
using namespace std ;
int main(){
	char s[3000];

	scanf("%s",s);
	while(1){
		stack<char> stack;
		if(s[i]=='{')
			stack.push(input[i]);
		else
			
	}
	return 0;
}
